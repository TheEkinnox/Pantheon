# set the project name
get_filename_component(CURRENT_FOLDER_NAME ${CMAKE_CURRENT_LIST_DIR} NAME)
set(PROJECT_NAME ${CURRENT_FOLDER_NAME})

include_external_msproject(
	${PROJECT_NAME} ${CMAKE_CURRENT_SOURCE_DIR}/Editor.csproj
	TYPE FAE04EC0-301F-11D3-BF4B-00C04F79EFBC
	PLATFORM AnyCPU
)

add_dependencies(${PROJECT_NAME} ${ENGINE_API_NAME})
add_dependencies(${PROJECT_NAME} ${EDITOR_API_NAME})

set(EDITOR_NAME ${PROJECT_NAME})